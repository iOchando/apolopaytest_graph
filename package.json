{
  "name": "apolopaytest",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ iochando/apolopaytest",
    "create-local": "graph create --node http://localhost:8020/ iochando/apolopaytest",
    "remove-local": "graph remove --node http://localhost:8020/ iochando/apolopaytest",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 iochando/apolopaytest",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.61.0",
    "@graphprotocol/graph-ts": "0.30.0"
  }
}
